services:
  mighty-ai-qr-web:
    build: .
    container_name: mighty-ai-qr-web
    restart: unless-stopped
    ports:
      - "3005:3000"
    environment:
      API_URL: http://mighty-ai-qr-server:3003
    networks:
      - proxy_net

networks:
  proxy_net:
    external: true
